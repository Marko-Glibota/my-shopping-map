<h1><%= @shop.name %></h1>

<p>Address: <%= @shop.address %></p>

<%= link_to "Edit shop", edit_shop_path(@shop) %>

<%= link_to "Delete shop", shop_path(@shop),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
